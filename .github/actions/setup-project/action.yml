name: "Setup Project"
description: "Reusable Node.js 18 setup and install"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v3
      with:
        node-version: 18
    - run: npm ci
      shell: bash
